workflows:
  teacher-release-workflow:
    name: Teacher App Release (APK)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Teacher Release
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird release android --flavor teacher -t lib/main_teacher.dart --artifact apk --no-confirm
    artifacts:
      - build/app/outputs/flutter-apk/app-teacher-release.apk
      - build/app/outputs/bundle/teacherRelease/app-teacher-release.aab

  teacher-patch-workflow:
    name: Teacher App Patch (Update)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Teacher Patch
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird patch android --flavor teacher -t lib/main_teacher.dart --no-confirm

  student-release-workflow:
    name: Student App Release (APK)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Student Release
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird release android --flavor student -t lib/main_student.dart --artifact apk --no-confirm
    artifacts:
      - build/app/outputs/flutter-apk/app-student-release.apk
      - build/app/outputs/bundle/studentRelease/app-student-release.aab

  student-patch-workflow:
    name: Student App Patch (Update)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Student Patch
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird patch android --flavor student -t lib/main_student.dart --no-confirm

  admin-release-workflow:
    name: Admin App Release (APK)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Admin Release
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird release android --flavor admin -t lib/main_admin.dart --artifact apk --no-confirm
    artifacts:
      - build/app/outputs/flutter-apk/app-admin-release.apk
      - build/app/outputs/bundle/adminRelease/app-admin-release.aab

  admin-patch-workflow:
    name: Admin App Patch (Update)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Admin Patch
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird patch android --flavor admin -t lib/main_admin.dart --no-confirm

  universal-release-workflow:
    name: Universal App Release (APK)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Universal Release
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird release android --flavor universal -t lib/main.dart --artifact apk --no-confirm
    artifacts:
      - build/app/outputs/flutter-apk/app-universal-release.apk
      - build/app/outputs/bundle/universalRelease/app-universal-release.aab

  universal-patch-workflow:
    name: Universal App Patch (Update)
    max_build_duration: 30
    environment:
      groups:
        - shorebird
      flutter: stable
    working_directory: flutter_mobile
    scripts:
      - name: Install Shorebird
        script: |
          curl --proto '=https' --tlsv1.2 -sSf https://raw.githubusercontent.com/shorebirdtech/install/main/install.sh | bash
          export PATH="$HOME/.shorebird/bin:$PATH"
      - name: Build Universal Patch
        script: |
          export PATH="$HOME/.shorebird/bin:$PATH"
          flutter clean
          shorebird patch android --flavor universal -t lib/main.dart --no-confirm
