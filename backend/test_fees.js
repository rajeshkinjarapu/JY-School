const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.feePayment.findMany({ where: { status: 'PENDING' }, include: { student: { select: { rollNo: true, user: { select: { name: true } }, class: { select: { name: true, section: true } } } }, feeStructure: { select: { name: true, amount: true } } } }).then(console.log).catch(console.error).finally(() => prisma.$disconnect());
